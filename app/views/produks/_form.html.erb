<%= form_for(@produk) do |f| %>
  <% if @produk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produk.errors.count, "error") %> prohibited this produk from being saved:</h2>

      <ul>
      <% @produk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br />
    <%= f.text_field :nama %>
  </div>
  <div class="field">
    <%= f.label :alamat %><br />
    <%= f.text_field :alamat %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
