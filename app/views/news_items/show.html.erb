<div class="contentbox">  
  <article class="news_item">
    <h2><%= @news_item.title %></h2>
    <small><%= t('.published') %> <%= l(@news_item.publish_date, :format => :long) %></small><br>
    <%= @news_item.body.html_safe %>
    <p><%= link_to t('.back_to_index'), news_items_url %></p>
  </article>
</div>
<div class="sidebar"><%=raw @page[:side_body] %>  
</div>
